# Application
spring.application.name=deploytrack

# Server
server.port=8080

# ---- Database (H2 for dev) ----
spring.datasource.url=jdbc:h2:mem:deploytrack;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# ---- JPA ----
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

# ---- IBM UrbanCode uDeploy ----
udeploy.base-url=https://your-udeploy-server.example.com
udeploy.auth-token=YOUR_UDEPLOY_API_TOKEN
# Set to true to skip SSL verification in dev (not recommended for prod)
udeploy.ssl-verify=false

# ---- CORS ----
cors.allowed-origins=http://localhost:5173
